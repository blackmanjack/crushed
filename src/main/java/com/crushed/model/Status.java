package com.crushed.model;

/**
 * POTENTIAL = passive hypothesis, not yet proven.
 * CONFIRMED = proven via active probe success or an OAST callback.
 */
public enum Status {
    POTENTIAL, CONFIRMED
}
